// PWAPrompts.tsx — Disabled as part of PWA feature removal.
// The install prompt no longer applies to this application.
// Retained as a no-op to avoid breaking any existing imports.

export default function PWAPrompts() {
  return null;
}
