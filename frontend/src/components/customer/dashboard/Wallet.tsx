import { motion } from 'framer-motion';

export default function Wallet() {
  return (
    <div className="flex flex-col gap-6">
      <h2 className="text-2xl text-white font-bold">Wallet</h2>
    </div>
  );
}
